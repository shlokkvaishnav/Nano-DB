version: "3.8"

services:
  nanodb:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - nanodb-data:/app/data
    environment:
      - NANODB_PORT=8080
    restart: unless-stopped

volumes:
  nanodb-data:
